package artery.ntn;

simple LEO_SatelliteManager
{
    parameters:
        @display("i=block/cogwheel");
        string satelliteName = default("");
        int planeId = default(0);
        int satId = default(0);
}
